<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:dist="http://schemas.android.com/apk/distribution"
	xmlns:tools="http://schemas.android.com/tools"
	package="com.contest">

	<application
		android:allowBackup="true"
		android:fullBackupContent="true"
		android:icon="@mipmap/ic_launcher"
		android:label="@string/app_name"
		android:roundIcon="@mipmap/ic_launcher_round"
		android:supportsRtl="true"
		android:theme="@style/AppTheme"
		tools:ignore="GoogleAppIndexingWarning">
		<activity android:name="com.contest.controller.QuizReportActivity" />
		<activity android:name="com.contest.controller.MyReportActivity" />
		<activity android:name="com.contest.controller.SearchActivity" />
		<activity android:name="com.contest.controller.CreateQuestionsActivity" />
		<activity android:name="com.contest.controller.NewQuizActivity" />
		<activity android:name="com.contest.controller.RegisterActivity" />
		<activity android:name="com.contest.controller.HomeActivity" />
		<activity android:name="com.contest.controller.AnswerActivity" />
		<activity android:name="com.contest.controller.LoginActivity">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />

				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>
	</application>

	<dist:module dist:instant="true" />

</manifest>